export default function New(userId, name, lastName, dni, type, token) {
    this.userId = userId,
    this.name = name, 
    this.lastName = lastName, 
    this.dni = dni,
    this.type = type,
    this.token = token
}